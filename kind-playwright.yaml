apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv01-kind-worker
  labels:
    name: pv01
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 100Mi
  storageClassName: standard
  local:
    path: /some/path/in/container # Replace with the path to your local storage
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - kind-worker
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv02-kind-worker2
  labels:
    name: pv02
spec:
  accessModes:
    - ReadWriteOnce
  capacity:
    storage: 100Mi
  storageClassName: standard
  local:
    path: /some/path/in/container # Replace with the path to your local storage
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - kind-worker2
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc01
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
  selector:
    matchLabels:
      name: pv01
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: virgilius-deployment
  labels:
    app: virgilius-app
spec:
  selector:
    matchLabels:
      app: virgilius-app
  replicas: 1 # tells deployment to run 2 pods matching the template
  template:
    metadata:
      labels:
        app: virgilius-app
    spec:
      volumes:
      - name: storage
        persistentVolumeClaim:
          claimName: pvc01
      containers:
      - name: virgilius-container
        image: ubuntu:22.04
        command: ["/bin/sh", "-c", "chmod 777 /some/mount/path/install_playwright.sh && cd /some/mount/path && ./install_playwright.sh && sleep 3600"]
        volumeMounts:
          - name: storage
            mountPath: /some/mount/path
        ports:
        - name: http
          containerPort: 80
        env:
        - name: DEBIAN_FRONTEND
          value: noninteractive
---
apiVersion: v1
kind: Service
metadata:
  name: kind-loadbalancer
spec:
  selector:
    app: virgilius-app
  type: LoadBalancer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
